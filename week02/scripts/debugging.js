let radius = 10;
let area = 0;

let radiusOutput = document.textContent = radius;
let areaOutput = document.textContent = area;

const PI = 3.14159;

area = PI * radius * radius;
radiusOutput = radius;
areaOutput = area;

radius = 20;
area = PI * radius * radius;
radiusOutput = radius;
areaOutput = area;